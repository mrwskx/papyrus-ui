import { Meta, ArgTypes, Canvas } from '@storybook/addon-docs';
import Range, {
  Basic,
  Description,
  Disabled,
  Invalid,
  Message,
} from './range.stories';

<Meta of={Range} />

# Range

Input of type range

## When To Use

- When user needs to select a value from a range.

## Examples

### Basic

<Canvas of={Basic} />

### Description

Displays description above the input. The description can be used for additional information or instructions for the
user.

<Canvas of={Description} />

### Message

Displays a hint message below the input. This message can be used as a lower-priority feedback when no validation error
has been occurred.

<Canvas of={Message} />

### Invalid

Setting the `invalid` property to `true` indicates an error or issue with the input value.

<Canvas of={Invalid} />

### Disabled

Disabled input

<Canvas of={Disabled} />

## API

<ArgTypes />
